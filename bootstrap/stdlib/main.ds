extern {
    fn printf(x: str);
}

fn foo(x: i32) : i32
{
    if(x > 7)
    {
        return 50;
    }

    return 100;
}


fn main()
{
    printf("a: %d\n", foo(10));
    printf("a: %d\n", foo(5));
}