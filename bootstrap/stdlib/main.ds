extern {
    fn printf(x: str);
}

fn main()
{
    var x = 0;

    loop(5)
    {
        printf("loop %d\n", x);
        x = x + 1;
    }

    var z = 0;

    loop(z < 7)
    {
        printf("while loop %d\n", z);
        z = z + 1;
    }
}