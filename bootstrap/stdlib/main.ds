extern {
    fn printf(x: str);
}

@inline
infix op +(a: i32, b: u8) : i32
{
    i_add();
}

@inline
infix op +(a: u8, b: i32) : i32
{
    i_add();
}

fn main()
{
    printf("Hello World!\n");

    let x = 10;
    let y = 5u8;
  
    let z = x + y;

    printf("%d\n", z);

}