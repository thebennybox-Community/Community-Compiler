extern {
    fn printf(x: str);
    fn fopen(filename : str, mode : str) : u32;
    fn getc(stream : u32) : i32;
    fn fclose(stream : u32) : i32;
}

struct point
{   
    x: u32
    y: u32
    z: u32
}


fn main()
{
   var x = [10, 15, 4];
   printf("%d, %d, %d\n", x[0], x[1], x[2]);

   printf([0x000A6948])
}
