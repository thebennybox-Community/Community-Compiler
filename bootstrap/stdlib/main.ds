
extern {
    fn putchar(ch: u8) : i32;
    fn printf(x: str);
    //fn printf(x: str, d: u8);
}


fn main()
{
    printf("Hello World!");
    putchar(10);

    printf("%d", (16) / 2); 
    
    if(10 == 15)
    {        
          
    }

    putchar(10);
}