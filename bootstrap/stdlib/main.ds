extern {
    fn printf(x: str);
}

fn main()
{
    var x = 0;

    loop(5)
    {
        printf("loop %d\n", x);
        x = x + 1;
    }


    loop(5 == 5)
    {
        printf("while loop\n");
    }
}